# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# envs
.venv*/
env/
venv/

# Hydra / experiment run dirs
outputs/
multirun/

# heavy, regenerable artifacts
checkpoints/**
*.ckpt
*.pt
*.pth
mlruns/

# data — keep only the tiny toy fixture used by test_smoke
data/**
!data/toy/
!data/toy/**

# keep the science (metrics, certificates, exported circuits, figures)
!results/**
!figures/**

# reference PDFs: keep local for reading, don't bloat the repo (index is in references/README.md)
references/pdfs/

# anonymized snapshot artifact (regenerate via scripts/make_anon_snapshot.sh)
simcert_anon_code.zip

# LaTeX build artifacts
paper/*.log
paper/*.aux
paper/*.out

# arXiv build artifacts
/arxiv/
/arxiv_submission.tar.gz

# grid driver logs (regenerable; the science lives in results/)
grid.log
rerun_grid.log
grid_sequential.log
