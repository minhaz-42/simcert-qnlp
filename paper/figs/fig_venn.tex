% Fig 2: the trained states themselves, one per audited case, as the objects that decide
% the verdict; then the class the audit certifies. The Schmidt spectra are measured on the
% audited configurations, not illustrative. Regenerate with
%   python figures/scripts/fig_objects.py
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{objects/schmidt_all.pdf}

\vspace{1.5mm}

\begin{tikzpicture}[
  font=\small,
  box/.style={rounded corners=3pt, minimum height=13mm, minimum width=28mm, align=center,
              line width=0.8pt},
]
\node[box, draw=teal!55!black, fill=teal!12] (csim)
  {\textbf{CSIM}\\[3pt]\footnotesize classically\\[-1pt]\footnotesize simulable};
\node[box, draw=blue!50, fill=blue!7, right=11mm of csim] (qesim)
  {\textbf{QESIM}\\[3pt]\footnotesize classical up\\[-1pt]\footnotesize to data loading};
\node[box, draw=black!45, fill=black!4, right=11mm of qesim] (qsim)
  {\textbf{QSIM}\\[3pt]\footnotesize quantum-\\[-1pt]\footnotesize simulable};
\node at ($(csim.east)!0.5!(qesim.west)$) {\large$\subseteq$};
\node at ($(qesim.east)!0.5!(qsim.west)$) {\large$\subseteq$};
\node[below=6mm of csim, align=center, font=\footnotesize, black!75] (note)
  {all five models, all four datasets\\[-1pt]$\chi^\star\!\le 2$ in every one of the 129 runs};
\draw[->, black!55] (note) -- (csim);
\end{tikzpicture}
\caption{The states that decide the verdict, and the class the verdict names. Top, the
Schmidt spectrum at the central cut of one trained state per audited case, measured on the
audited configurations. The solid rule marks where a $\chi{=}1$ surrogate truncates and the
dotted rule where $\chi{=}2$ does, so each panel shows directly how much of the state is
being discarded. The compositional \texttt{discocat} is the informative one: its spectrum
carries two weights and then falls off a cliff, so it is almost exactly rank two, which is
why it is the one model in the paper that needs $\chi{=}2$ and cannot be recovered by a
product state. The attention model is near rank one already. The reference classifier keeps
only $0.44$ of its weight in the leading Schmidt value on \textsc{mc} and $0.61$ on
\textsc{sst-2}, so its state is genuinely spread, and yet its decisions survive truncation
to a product state, which is the dissociation this paper reports. Weights below $10^{-7}$
are clipped so the \texttt{discocat} cliff does not flatten the other panels. Bottom, the
nested hierarchy: a classically simulable model (CSIM) is a special case of one simulable
up to a data-loading step (QESIM), itself a special case of quantum-simulable (QSIM)
\citep{cerezo2025,shin2024}. \textsc{SimCert} certifies membership in CSIM whenever the
trained state truncates to a small bond dimension, and we make no claim outside that
innermost class.}
\label{fig:venn}
\end{figure}
